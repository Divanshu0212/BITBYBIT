import React from 'react';

// AgentScorer is no longer needed as a separate sidebar component.
// Freelancer scoring/assignment is now handled directly in EmployerDashboard.
// This is kept as an empty export to prevent import errors.
export default function AgentScorer() {
  return null;
}
