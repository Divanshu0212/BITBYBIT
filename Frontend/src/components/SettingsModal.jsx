import React from 'react';

// SettingsModal is no longer needed — auth handles everything.
// Kept as empty export to prevent import errors if referenced elsewhere.
export default function SettingsModal() {
  return null;
}
